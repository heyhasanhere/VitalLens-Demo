# Lighting classifier training
#
# PyTorch — CPU build below; for GPU replace with the CUDA wheel:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install onnxruntime-gpu>=1.18.0
torch>=2.2.0
torchvision>=0.17.0     # MobileNetV3 backbone

# Data handling
numpy<2
opencv-python>=4.9.0
scikit-learn>=1.5.0

# ONNX export + validation (export_onnx.py)
onnx>=1.16.0
onnxruntime>=1.18.0

# Experiment tracking (optional — train.py skips silently if not installed)
# clearml>=1.16.0
